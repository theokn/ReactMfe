import { ShowMessage } from "marketing/ShowMessageApp";
import React, { useRef, useEffect } from "react";

export default () => {
  return <ShowMessage message="message in container"></ShowMessage>;
};
